#include <stdio.h>
void main(void)
{
    int num;

printf("Enter Number %d\n", num);
scanf("%d", &num);
    if(num / 2 = 1)
        printf("This number is odd");
    else(num / 2 = 0)
        printf("This is a even number");
}
